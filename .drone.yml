kind: pipeline
type: docker
name: build-and-test

trigger:
  branch:
    include:
      - feature/ci-test

steps:
- name: clone
  image: plugins/git
  settings:
    depth: 50

- name: test
  image: golang:1.19
  commands:
    - go version
    - echo "Running simple tests"
    - go test -v ./...
